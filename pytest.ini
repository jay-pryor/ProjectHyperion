[pytest]
# Framework-repository tests only (tooling/tests). The example project has its own
# pytest.ini. No cache dir: build_registry.py scans every *.md and would find its README.
addopts = -p no:cacheprovider
testpaths = tooling/tests
