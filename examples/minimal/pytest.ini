[pytest]
# --junitxml: every run writes trace/results.xml, which check_traces.py reads for
# outcome as well as existence (CORE-TRC-003). Generated; never committed.
addopts = --import-mode=importlib -p no:cacheprovider --junitxml=trace/results.xml
testpaths = modules validation
